import React, { useState } from 'react';
import { Plus } from 'lucide-react';
const faqs=[['Do I need to book in advance?','No booking needed for drop-off. For delivery or large orders, message us first.'],['How will I know when my order is ready?','We WhatsApp you as soon as your items are ready for collection or delivery.'],['What if something gets damaged?','We handle every garment with care. In the rare event of an issue, we will contact you immediately and work to make it right.'],['Do you do bulk orders for businesses?','Yes — we work with hotels, guest houses, salons and offices. WhatsApp us to discuss a custom arrangement that fits your schedule and volume.'],['What payment methods do you accept?','Cash, card, EFT and SnapScan in store.']];
export default function FaqSection(){const [open,setOpen]=useState(0);return <section className="faq-section"><div className="site-container narrow"><span className="eyebrow">Quick questions</span><h2>Good to know.</h2>{faqs.map(([q,a],i)=><article key={q}><button onClick={()=>setOpen(open===i?-1:i)} aria-expanded={open===i}>{q}<Plus className={open===i?'rotate':''}/></button>{open===i&&<p>{a}</p>}</article>)}</div></section> }